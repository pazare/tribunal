# Master Plan Adversarial Delphi Review — Ratified Verdict (2026-07-18)

**Target:** `TRIBUNAL_CLINICAL_FROM_SCRATCH_POC_MASTER_PLAN_2026-07-18.md`. **Method:** blind round → anonymized rotated reveal → on-record revision with change certificates → this ratification (every location quote re-grepped; ~30 passages spot-read: all BLOCKERs, every rebutted finding). **Panel:** CLIN clinical realism/safety · EF execution feasibility · RPC rules/provenance/citations · TA technical architecture · J judge/pitch adversary · F clarity/specificity auditor.

## Verdict

The plan is extraordinarily specific — nearly all 72 ratified defects are collisions between precise passages, not vagueness — but it is **not executable as written at 10:30**. Eleven BLOCKERs would fail its own pre-registered tests or put falsehoods before judges: the hashed CT-16 row auto-falsifies a correct run; the frozen verifier rejects every sanctioned 4-seat or non-vote run; the Tier-1 emergency packet always renders BLOCKED; quorum is defined nowhere; the WATCH event mapping breaks its own closure test; the after-cutoff check blocks the hashed twin; worktrees never see the frozen schema; two publication defaults contradict; three rehearsed scripts (answer 11, the heparin narration, beat 5's live arithmetic) assert things the build will not do. Every fix is a text edit; contract fixes must precede the 12:00 freeze, pre-registration fixes the twin-table hashing — applied, the plan is executable standalone. Gravest residual risks: rung-1 timing (protocolized, still tight), the L5 16:30–18:30 stack, the unverified judging-slot length. Panel process: 41/41 certificate-backed changes, zero capitulation flags.

## Ratified findings with fixes (this IS the apply list)

Entry: **Rnn** (sources;severity;C/S;support/6; +R = sole author, ratifier-verified) part "location" — issue. ✎ edit.

### Blockers

- **R01** (CLIN-1+F5;B;C;5/6) IV §4.4 "REQUEST_DATA → ESCALATE (MFM), urgency increases" — two-span base (§3.4); enum cannot flip; falsifier fails a correct run; polarity unstated. ✎ Expected-delta cell → "Both base spans retained (§3.4); ESCALATE urgency rises — urgencyTier 3→2 — REQUEST_DATA list narrows to confirmatory labs"; falsified-if → "ESCALATE absent, urgencyTier not lower, REQUEST_DATA list not smaller, or certificate absent"; §3.4 ticket += "urgencyTier orders 1 (most urgent) to 4; urgency increases = tier decreases"; align metric 8's example; beat 6 → "Change a decisive fact and the escalation urgency and missing-data list change, with a certificate"; §7.4 → "raises the escalation urgency with a certificate".
- **R02** (TA-06+F1;B;C;5/6) III §3.4 "before six `blind_opinion_committed`" — sanctioned 4-seat packs (M4, risk 6) and non-votes reveal after fewer commits; ABSTAIN carrier missing. ✎ Example → "an `opinions_revealed` before every roster voting seat has a `blind_opinion_committed` or recorded non-vote (`model_receipt_recorded` valid:false + injected ABSTAIN as `challenge_recorded` phase:'blind_nonvote', exempt from challenge ordering)"; test 6 → "reveal before all roster seats committed-or-non-voted → invalid (roster size from pack config, never hard-coded)".
- **R03** (TA-01;B;C;5/6) V §5.3 "only the fields the gate and one Haiku extraction can populate" — only runId optional; "critical flags" not a field; every Tier-1 packet BLOCKs. ✎ §3.4 EscalationPacket += `variant: 'emergent'|'full'`; evidencePage, optionsPage, problemRepresentation, collaborationRequested, closeLoop, attachments, runId required only when 'full'; §5.3: "critical flags" → gateStatus.reasons.
- **R04** (TA-02+F7;B;C;5/6) III §3.5 "the pack's quorum rule" — load-bearing in five places; defined nowhere. ✎ §3.7 pack config += `quorum: number` (six-seat pack 4; descope pack 3); §3.5 += "Quorum satisfiable iff valid voting seats >= quorum; voting seats = roster minus Ratifier; below → UNDERDETERMINED → STOP."
- **R05** (TA-04+F6;B;C;4/6) III §3.8 "each emits its ledger event (35-37)" — route/event off-by-one; resulted/actioned eventless; M9.5 violates test 35. ✎ Pre-freeze union → 40 kinds: add `result_available` {recordId, resultRef}, `result_actioned` {recordId, digest}; route row → "ack→35, action→result_actioned, close→37"; add POST /api/watch/:id/result; bump all four 38-counts; II.5 i10 → §3.4 enum order; insert result_actioned into M9.5.
- **R06** (CLIN-8+TA-03;B;C;4/6) III §3.6 s5 "after_cutoff" — date-granularity semantics undefined; gate BLOCKs the hashed twin's intra-day timestamps. ✎ Parenthetical → "after_cutoff fires iff the resource instant, truncated to date in its own recorded offset, is strictly after decisionCutoff — date-only cutoffs include the whole encounter date, so CT-16's 2019-09-27T10:12:00-07:00 is in-window"; §4.4(d) += "(chronology semantics per §3.6 step 5)".
- **R07** (EF-01;B;C;4/6) VI.2 "you merge into `main` at gates" — no reverse flow; worktrees never see the frozen schema. ✎ VI.2 += "Reverse flow: at the 11:20 draft announcement merge lane/l1 into main; immediately after EVERY merge to main run `git -C ../tc-<lane> merge main` for each active consumer lane; lanes never fetch one another's branches."
- **R08** (RPC-01;B;C;3/6) IV §4.5 "only the minimal case-16 excerpts" — contradicts VI.1.2 q3 "Dataset stays OUT"; echoed twice. ✎ Clause → "and NO record-derived content of any kind until answer #3 authorizes it — demo screens render from local TC_DATA_DIR; screenshots/submission video stay out of public artifacts until answer #3 permits"; align B.5 (delete "minimal excerpts").
- **R09** (CLIN-3+J2;B;C;3/6) VII a11 "the console asks for the clinician's commitment" — feature exists in no build item; scripted falsehood. ✎ Final clause → "so the packet's fixed ordering forces evidence before panel options (page 1 before page 2, §5.3), and the panel's sealed blind phases apply the same commit-first principle internally" (or build it: named P1 item, §5.3/§3.9, owner — decide pre-rehearsal).
- **R10** (CLIN-2+RPC-15;B;C;4/6) I 1.3.2/§1.8 "what the day-of case pack will author for her" — §4.3.2's closed list has no heparin; conflicting specs; narration scripted (1.1 p3). ✎ 1.3.2 parenthetical → "(…; the HIT continuation below is an illustrative composite only — the day-of pack authors the §4.3.2 scenario, no heparin data)"; §1.8 bullet B → "…a documented deliberation with the 14-month creatinine decline and the aspirin interaction surfaced by the factorizer instead of lost in a PDF."
- **R11** (J1;B;C;5/6) II.4 "Live run if the venue allows" — §3.7 budgets make a beat-1 start miss beat 5's 2:00. ✎ II.4 intro += "Rung-1 protocol: Santiago starts the run ~2 min before the slot; beats 1–3 narrate completed phases, beat 4 catches the live re-vote, beat 5 the live ratification; Pablo: 'this run started two minutes ago while we walked up — the timeline preserves every phase.' Never start at beat 1 — §3.7's budgets cannot land Stage B by 2:00."

### Majors

- **R12** (RPC-05+F4+EF-16;M;C;6/6) M9 + VI.3.5 "DOB 1961-03-14" — §4.3.2 reconciles to 1963. ✎ M9 → "63F, DOB 1963-03-14 (reconciled per §4.3.2; the 1961 defect lives only in the corrupted twin copy)"; VI.3.5 context → "DOB 1963-03-14 per §4.3.2 — the export's 1961 is the deliberate defect class, never authored forward"; L5 context += "§4.3.2's authored-pack spec (paste it)".
- **R13** (CLIN-7+TA-11+F2+F3+F16;M;C;5/6) IV §4.4 "\"source_key\": \"Observation\"" — patch keys/paths/ops off-schema. ✎ Both `"source_key"` → `"sourceKey"`; NT-16 path → `/derivedDocuments/0/text`; SD-16 → `[{"op":"add","path":"/regime/r2Assignments/specialty","value":"bundle-A"},{"op":"replace","path":"/regime/level","value":"R2"}]` (mirror B) — before hashing.
- **R14** (CLIN-4;M;C;4/6) IV §4.3.2 "Stage B ratifies a safety-flag Clinical Commitment Span" — VETO criterion absent; a VETO forbids the expected outcome. ✎ §4.3.2 + L3 context: "Life-Saver: VETO only for imminent-harm actions that must not proceed in any form (verified-allergy administration, wrong-patient/wrong-drug-class); documented interaction or organ-risk hazards are CONTRAINDICATION — ratifiable as safety-flag COMMIT_SPAN + ESCALATE as expected here."
- **R15** (TA-08;M;C;3/6) III §3.4 "verifierReport: string" — verifier unowned; allergy_conflict unproduced (fix per RPC rebuttal). ✎ §3.6 += "Span/packet verifier (packages/gate, pure verifySpan): units (UCUM allowlist; failure = unit_unknown, warning), chronology vs decisionCutoff, allergy_conflict (block) by intersecting snapshot AllergyIntolerance with medication mentions, citation existence; result written into span_ratified as optionsPage.verifierReport; runs on every Tier-1 packet."
- **R16** (CLIN-5;M;C;3/6) IV §4.4 NT-16 "noncompliant…refuses" — adherence facts in a framing-only lexicon. ✎ Lexicon → {claims, insists, poor historian, probably exaggerating}; delete noncompliant/refuses; fix the illustrative rewrite; checker rule "no adherence/behavior/consent assertions absent from the base transcript"; update M8's example and metric 9.
- **R17** (CLIN-6;M;C;3/6) VII a12 "No alerts." — falsified by §5.6's nine notify conditions. ✎ → "No alert stream: one packet per case, and a human is interrupted only under nine printed materiality conditions, each logged as notification_emitted and auditable (metric 14) — thresholds untuned and disclosed as such."
- **R18** (CLIN-9;M;C;3/6) V §5.2 "packet amends when ratification lands" — match-time pre-read unspecified. ✎ Tier-2 row += "Pre-read at match time = the §5.3 emergent-variant subset, valid under the packet-variant discriminant; ratified evidencePage then optionsPage arrive as packet_amended, preserving page-1-before-page-2."
- **R19** (CLIN-10;M;C;3/6) V §5.2 "the tier is re-evaluated" — vs §1.4(d)'s immutable tier; two producers. ✎ Worked example += "(packet.latencyTier records the escalation channel at trigger time — here 3 — and may differ from the immutable declared tier — here 2/FOCUSED; both ledgered via ConsultationTempoDecision.mappedTier. Amend §1.4(d) accordingly. Re-evaluation opens a NEW EscalationTicket; envelopes never mutate.)"
- **R20** (CLIN-11;M;C;3/6) V §5.2 "emergent-pattern flag" — defined nowhere. ✎ Triggers += "The emergent-pattern flag is deterministic and ledgered: true iff declared tier is 1 (§1.4), the router's tempo_selected is NOW (§3.5), or a clinician sets the explicit emergent toggle (human_initiated); no model output may set or clear it."
- **R21** (TA-05;M;C;4/6) III §3.4 e8 "opinionIds, per-recipient rotation seeds" — salts/bodies undisclosed; seals unverifiable. ✎ Event 8 payload → "full ClinicalOpinionObject per seat, its commit salt, and per-recipient rotation seeds; verifier recomputes each event-7 commitment from payload+salt, fails on mismatch."
- **R22** (TA-07;M;C;2/6) III §3.8 "No database on Saturday" — run-less events homeless. ✎ += "Case-scoped/pre-run events append to runs/case-<caseId>/ledger.jsonl with runId: null (envelope type Id | null); /api/events?caseId= merges case+run ledgers by ts; /api/verify accepts either."
- **R23** (TA-09;M;C;2/6) III §3.4 "seatsFor: number; seatsTotal: number" — support mapping and ratifierRule registry undefined. ✎ Decision-multiplicity += "A seat supports a span iff its final position equals the span's decision OR its dispositionWeights place >=0.3 on it; seatsTotal = voting-seat count; ratifierRule ids majority_no_veto | veto_constrained_escalate | underdetermined_stop | multi_span_composite (case-16)."
- **R24** (TA-10;M;C;2/6) IV §4.4 "mustChange: string[]" — untyped invariants. ✎ Invariants → typed predicates {path, op: 'unchanged'|'equals'|'decreases'|…}; restate delta cells in them before adjudication.
- **R25** (TA-12;M;C;3/6) III §3.8 "every transition ledgered" — confirm/close/unsigned-exit eventless. ✎ Lifecycle row += "confirm emits human_decision_recorded {decision:'match_confirmed'}; ticket close / unsigned exit emit {decision:'ticket_closed'|'consult_exited_unsigned'} — no new kinds."
- **R26** (TA-13;M;C;3/6) III §3.7 "replaying canonical outputs keyed by" — brittle keys; miss/authorship unassigned; rung 3. ✎ += "lookup (seatId, stage, phase, sha-prefix) then (seatId, stage, phase), receipted fixtureFallback:true; full miss fails closed printing the key; L3 hand-authors the case-16 fixture set at 12:30."
- **R27** (F8;M;C;5/6) IV §4.3.3 "Cardiology (`activeLoad` at `capacityCap`" — Thornton match dead-ends; beat 7 scripts acceptance. ✎ Either add roster entry 9 "Cardiology on-call (passes every hard filter) — accepts the Thornton consult", update the three "8 specialists" counts, name the acceptor in M10/beat 7; or script beat 7 on case 16's MFM consult and stage Thornton as rehearsed `escalation_unfilled`. Pick one.
- **R28** (EF-02;M;C;4/6) VI.3.4 "case browser, gate-report view…" — M3 canvas, M9 tiles unbuilt. ✎ Mission += "evidence canvas (II.5 i1), metric tiles (II.5 i6)"; milestones += "13:45–14:10 evidence canvas from L2's factorizer output (M3 14:15)", "18:15 metric tiles reading runs/…/eval/ JSON (M9 18:30)".
- **R29** (EF-03;M;C;4/6) VI.4 "Gate v1 on case 16 :crit, l2c, 12:00, 60m" — build bar vs G1 criterion. ✎ Gantt → "Gate v1 green (G1) :crit, l2c, 11:45, 15m" + "Full gate checks :l2c2, 12:00, 60m" (or G1 cell → "12:00 freeze / 12:15 gate-v1 green").
- **R30** (EF-04;M;C;2/6) VI.3.3 "route-registration hook" — no delivery milestone. ✎ Milestones += "13:00 endpoint freeze exports registerRoutes(app) with a compiling no-op escalation mount, so L5 never touches apps/server files."
- **R31** (EF-05;M;C;5/6) VI.3.3 "failing at 15:30 -> Pablo decides" — vs G2 row's 17:30. ✎ Trigger → "failing at 15:30 → alert Pablo, L8 arms rung-3 assets; BINDING rung-3 decision is 17:30 per the G2 row."
- **R32** (EF-06;M;C;4/6) VI.4 "Two soft buffers are built in" — both hold :crit bars. ✎ → "Real slack is 16:30–17:30 and 20:30–21:00; there is NO buffer before G2 — deferred feedback moves to 16:30–17:00"; amend Appendix C rule 6.
- **R33** (EF-07;M;C;3/6) VI.2 "descopes at gate time" — checkpoint descopes deciderless. ✎ Authority cell → "…descopes at gate times AND at any checkpoint whose descope this Part pre-commits (M3.5, 18:00 WATCH, M9.5); Pablo alone decides non-pre-committed cuts and 17:30/18:30."
- **R34** (EF-08;M;C;4/6) VI.1.1 "known local path" — path never named. ✎ Name the canonical dataset path in VI.1.1 item 2; start.json dataset block += "data_dir"; Step 2 += ".env with TC_DATA_DIR=<path>".
- **R35** (EF-09+F10;M;C;5/6) VI.3.7 "from the README alone" — README unowned. ✎ Step 2 write list += "README.md (title, synthetic banner, pre-existing-vs-day-of, Commands: install / pnpm gate --case 16 / pnpm tribunal / pnpm ledger verify / console URL)"; L1 upkeep; Commands refreshed at each gate merge; complete 16:30.
- **R36** (EF-10;M;C;3/6) VI.3.5 "the bounded WATCH slice beside the escalation service" — 16:30–18:30 overload. ✎ Dispatch Spec V-A as an L3-support subagent at 16:00 unconditionally; Santiago 16:30–18:30 = queue UI + WATCH only; dinner 17:30–18:00.
- **R37** (EF-11;M;C;4/6) II.2 "slips more than 45 minutes…descope ladder" — dangling name; 30 vs 45. ✎ → "If any checkpoint slips more than 30 minutes (matching risk row 8), apply row 8's strip order plus the owning gate row's descope — never borrow from M11–M13."
- **R38** (RPC-02;M;C;5/6) M13 "diff sequence from BUILD_MANIFEST_TEMPLATE" — old-repo-only tag; four stale pointers. ✎ M13 Run → "`git diff $(git rev-list --max-parents=0 HEAD)...HEAD --stat > runs/hackathon-20260718/day-of-diff.txt`"; VI.7 10:32 row → "section-header concept only — VI.1.4 supersedes the template"; repoint §3.3 and §7.4 at VI.1.3/VI.1.4.
- **R39** (RPC-03;M;C;3/6) VI.1.4 §6 "commands a judge can run from a fresh clone" — committed ledgers carry sponsor spans. ✎ §4.5 r3 + VI.1.4 §6 += "Until answer #3 authorizes excerpts, sponsor-derived runs/ ledgers are gitignored; §6 lists their commands as machine-local; judge-clone commands cover authored-pack runs only."
- **R40** (RPC-04;M;C;2/6) IV §4.3 "Nothing here is pre-built tonight" — content pre-designed here; arming absent. ✎ Preamble += "Content designs — trap dialogue, twin values, thresholds — originate in this disclosed pre-event plan (manifest §2.3); day-of work implements them as sealed, gate-passing data. If asked, say exactly that." Beat-8 line += "including the case-pack content designs".
- **R41** (RPC-06+F9;M;C;6/6) VI.1.5 + §1.8 "The Vite app shell, dark layout" — canon: light console. ✎ VI.1.5 i3 → "light clinical theme (II.5; dark = consult room only)"; §1.8 → "the light-themed clinician console".
- **R42** (RPC-08;M;C;2/6) VII §7.3 "boundary: paid NPDB claims" — anchors outside every register. ✎ B.4 += "Saber Tehrani 2013 NPDB figures (28.6% / 35.2% / $386,849) and GAO-19-292 median (~$36,400, 2017 billed charges) — re-verify from primary sources before the ROI card or any judge-ready sentence."
- **R43** (RPC-09;M;C;2/6) VI.7 "non-code artifacts reused as content" — DATA borrows invisible to header audits. ✎ VI.1.4 += "2.5 Old-repo artifacts reused as content (one row per VI.7 DATA row)"; VI.7 += "in-tree DATA files listed in §2.5; the 20:15 audit checks them both ways"; add a DATA row for SATURDAY_EXECUTION_PLAN's checklist (M13).
- **R44** (J3;M;C;1+R/6) VI.6 "Pablo chooses the rung" — mid-outage promise unoperationalized. ✎ += rung 2b: "Mid-demo network loss: Santiago switches to the bookmarked G4-rehearsal runId (tab pre-opened, rehearsed at M12), resumes at the same beat; Pablo: 'the venue network just dropped — this is this morning's receipted run; we verify its chain live at the end.'"
- **R45** (J4;M;S;3/6) VII a1 "answers in seconds on stage" — narrowed per CLIN rebuttal: unscheduled, unrehearsed, BLOCKs until R03. ✎ II.4 reserve line += "On 'show me latency': Santiago clicks the NOW card → human_initiated → Tier-1 packet renders with phase timer (≤10 s, §5.2); rehearsed at M12, recorded as an L8 asset. Gated on R03 pre-freeze; else narrate II.7's designed state over the M12 recording."
- **R46** (J5+EF-15;M;C;2/6) VI.4 G4 "cut consult-room first, then argument-graph" — cut #2 nonexistent; no short variant. ✎ G4 row + VI.3.8 → "then compress beat 1 to 15 s (open on the evidence canvas)"; II.4 += 2-minute variant: beats 2 (0:20), 3 (0:30), 5 (0:30), 8 (0:40); twins + consult room to booth.
- **R47** (J6;M;C;3/6) II.4 b4 "a seeded capitulation flag" — implied organic; unlabeled. ✎ Cell → "a seeded capitulation flag with an on-screen SEEDED TEST chip"; line += "we seeded this one so you can see the flag — real pressure-test counts are on slide 6."
- **R48** (J7;M;C;4/6) II.4 b3 "the panel cannot anchor itself" — exceeds the Yin boundary. ✎ → "no seat can anchor on a peer before it commits — and any change after must file its reason."
- **R49** (J8;M;C;2/6) VII §7.4 "Complex cases fail quietly" — 121 words ≈ 45 s. ✎ Replace with the ~83-word version (mechanism first; ends with the D26 tagline).
- **R50** (J9+CLIN-15;M;C;3/6) VII a6 "break-even is five and a half minutes" — realized-savings language; pre-committed "$4". ✎ Answer 6 → capacity framing + "converting it into dollars is the pilot's first question" + "model cost prints live from today's receipts, anchored three to six dollars"; Scenario A → "buys back its price in clinician capacity at".
- **R51** (J10;M;C;1+R/6) VII §7.5 "Michal (Abridge engineer, judge):" — buyer, authored-twins, injection unscripted. ✎ Append Q&A 20 (buyer: AI-governance committee; $25/case + $2,000/month anchors, H2V; silent-mode pilot motion), 21 (authored-twins defense incl. "content designs are written in our disclosed pre-event plan"), 22 (transcript-borne injection HYPOTHESIZED, disclosed; typed blind isolation covers peer text); 7.6 += row 13 "Document-borne prompt injection | HYPOTHESIZED | measurement path: silent-mode pilot + injection-twin family."
- **R52** (J11;M;C;2/6) II.4 b8 "chain OK -> tamper -> loud BREAK; disclosure slide" — overpacked 30 s. ✎ First verify runs in beat 7's last 5 s; beat 8 = tamper → BREAK only, disclosure slide already up; close on the "any commit you point at" line; command per R54.
- **R53** (J16;M;C;1+R/6) VII §7.4 "2:30-3:15 dashboards…baseline comparison table" — content diverges from II.4 after 2:30. ✎ Rewrite the overlay beat-indexed 1:1 with II.4 (twin b6/2:30, room b7/3:00, tamper+disclosure b8/3:30); baseline table into beat-6 narration; ROI card + limits + ask → slides/booth; add "II.4's beat table is canonical for screen content and clock."
- **R54** (F11+J13+F13;M;C;4/6) III §3.8 "`pnpm demo:tamper`" — off §3.2's canon; M11 correct. ✎ §3.8 → "`pnpm ledger tamper-demo`"; §3.1 → "`pnpm ledger verify <runId>`"; §3.11 → "`pnpm ledger verify`".

### Minors

- **R55** (CLIN-12;m;C;1+R/6) I 1.3.1 "Tier 2 while she is not crashing". ✎ → "Tier 2 while she is not crashing — if pressures confirm severe-range persistent, the binder's 30–60-minute protocol is already running as a bedside act (the escalate-and-transfer decision is the Tier-2 object) — and the instant she seizes it becomes Tier 1…".
- **R56** (CLIN-13;m;C;2/6) V §5.4 "same care team or prior involvement → recusal". ✎ Filter (iv) → "conflict of interest (any non-empty conflictFlags: same care team, prior involvement, or declared financial/payer affiliation → recusal)".
- **R57** (CLIN-14;m;C;2/6) IV §4.3.2 "catch what a rushed human plausibly would not". ✎ → "the audience watches the system flag a hazard that is documented in the chart but easy to miss under time pressure."
- **R58** (RPC-07;m;C;3/6) §3.7 + a3 "30-day retention requirement" (revised down on J's counter-evidence: risk 12 tagged, 7.6 r10 orders re-verification). ✎ Tag §3.7 Frontier row + answer 3 "[verify at check-in]"; scope risk-12's tag to the figure; add the B.3 retention item with a no-number fallback.
- **R59** (RPC-10;m;C;2/6) III §3.3 "Three modes - COPY, REFERENCE, REBUILD". ✎ → "Four dispositions — COPY, REFERENCE, DATA/TEXT, REBUILD (default) —…; Santiago's export additionally PRE-EVENT DESIGN ARTIFACT (manifest §2.2)", matching VI.7.
- **R60** (RPC-11;m;C;1+R/6) §3.3 header "section \"Borrowed substrate\"". ✎ → "disclosed in BUILD_MANIFEST section 4 (Borrowed Files)."; note VI.7's short form as accepted alternative.
- **R61** (RPC-12;m;C;4/6) VI.1.3 "shasum -a 256 \"<path-to-this-master-plan-file>\"". ✎ Both placeholders → the literal plan path; VI.1.1 item 3 names it the canonical hash target.
- **R62** (RPC-13;m;C;4/6) G0 "hash spot-check recorded". ✎ Dataset block += `"friday_spotcheck": { "at": null, "archive_sha256_matches": null }`; G0 → "…with dataset.friday_spotcheck filled".
- **R63** (RPC-14;m;C;4/6) G1 "run-gate --case 16". ✎ → "`pnpm gate --case 16`".
- **R64** (TA-14;m;C;2/6) III §3.7 "full UI experience from its JSONL alone". ✎ → "`pnpm replay <runId> --pace recorded|instant|step`; default recorded, spacebar advances phase boundaries; L4 reducer accepts bursts."
- **R65** (TA-15+F12;m;C;4/6) VI.3.3 "POST /api/run {caseId, regime}". ✎ → "`POST /api/run {caseId, regime, packId, config} -> {runId}` (409 on gate BLOCK)"; align the curl.
- **R66** (EF-12;m;C;4/6) G2 row "L6's support agent moves to L3". ✎ → "L5's support subagent (Spec V-A) moves to L3".
- **R67** (EF-13;m;C;3/6) VI.1.3 "git worktree add". ✎ Loop → append `&& git -C "../tc-$L" push -u origin "lane/$L"` (or push.autoSetupRemote), both occurrences.
- **R68** (J12;m;C;1+R/6) §7.3 "$25 = $3.70 measured…+ $21.30 platform fee". ✎ Slide 8 += "the only measured number is model cost printed live from today's ModelReceipts (anchored $3–6); the $21.30 platform fee is priced, not earned."
- **R69** (J15;m;C;3/6) II.4 "Pablo narrates; Santiago drives". ✎ → "Pablo narrates and drives beats 3, 4, 7 while Santiago speaks; Santiago drives all other beats — the person mid-UI-transition never speaks."
- **R70** (J17;m;C;1+R/6) VII a5 "can fire from the fast lane in seconds". ✎ → "Both: the fast lane hands you the gate's missing-data findings as a templated bounded request in seconds — deterministic, no panel — and the panel's ratified REQUEST_DATA/ESCALATE follows as a packet amendment; the full tribunal is for the complex differential — and it hands you the brief."
- **R71** (F14;m;C;4/6) II.1.3 "final values owned by Part III". ✎ §3.9 += "Ports, canonical: console 5173, consult-room 5174, both strictPort: true; server 4680 per §3.7."
- **R72** (F15;m;C;3/6) V §5.4 "a per-tier timeout auto-advances". ✎ += "(canonical demo values, HYPOTHESIZED, in packs/<pack>/exchange.json: Tier-1 30 s, Tier-2 60 s, Tier-3 15 min demo-scaled, Tier-4 none — scheduled slot)".

## Rejected and voided

**Voided quotes: 0** — all 91 location quotes found verbatim. **J14 (beat 2 "misstates Kwong") — REJECTED, two rebuttals sustained:** the plan's verified register (§1.7, §3.6, citations) locates the silent failure *inside* the clinician-blinded silent trial, caught pre-exposure; beat 2 says exactly that and never claims the trial failed — the rewrite is optional polish. **J4 premise — rebuttal sustained in part**, ratified narrowed (R45). **TA-08 fix premise — scoped rebuttal sustained:** the pack already ships AllergyIntolerance in open-typed `resources[]`; the `allergies: string[]` field is dropped, core stands (R15). **Self-withdrawals (2):** CLIN-15→R50, EF-14→R61 — duplicate merges, not attrition.

## Preserved dissent

1. **R10:** CLIN BLOCKER (conflicting specs + scripted falsehood) vs RPC MAJOR (demo path unaffected). Ratified BLOCKER per R09's falsehood class; fix identical.
2. **R04:** TA BLOCKER (risk-6 playbook makes divergence near-certain) vs F MAJOR (surfaces at build time). Ratified BLOCKER; F's integer-quorum fix adopted.
3. **R05:** TA BLOCKER (freeze-locked; even the fixture descope needs a ledger-legal sequence) vs F MAJOR (bounded P1). Ratified BLOCKER.
4. **R45:** CLIN calls the residue materially weaker; ratified MAJOR — scripted, unperformable until R03.
5. **R54:** F MAJOR vs J MINOR; ratified MAJOR (M11 guards "the single most disqualifying possible bug").
6. **J14:** maintained post-rebuttal; rejected, recorded as polish.

## Panel process integrity

41 status changes (CLIN 7R+1W; EF 3R+2N+1W; RPC 5R; TA 5R; J 10R+2N; F 4R+1N), all certificate-backed; the ratifier spot-verified the load-bearing ones (II.3's "fixed verification lag" for EF-03; risk-12's tag for RPC-07; §3.2's list for J13's flip; M4's floor for TA-06). **Evidence-based change rate 41/41 (100%); capitulation flags 0.** Softenings (RPC-07, RPC-14, EF-03) and escalations (R01, R02, R06, RPC-15) each cite quoted text — judgments moved both directions. No withdrawal-by-silence; nine-plus sole-coverage findings kept. Constitutional notes upward: state intra-Part-VI clock supremacy ("tables govern; gantt illustrative"); canon note that urgency lives on EscalationTicket.urgencyTier (1 = most urgent), never the enum; consider a TEST-INVALID-with-receipt disposition.

## Apply list

The ✎ edits above are the apply list: all BLOCKERs (R01–R11), all MAJORs (R12–R54), and the MINORs (R55–R72). **Sequencing:** R02–R06, R14–R15, R18–R26 before the 12:00 freeze; R01, R06, R13, R16, R24 before the twin table is hashed; R07, R34, R35, R61–R63, R67 at T-0; script edits (R09–R11, R17, R44–R54, R69–R70) before the 19:15 Q&A freeze / 19:45 rehearsal.
